type PageInfo {
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
  startCursor: String
  endCursor: String
}

interface Node {
    id: ID!
}

type Query {
  _empty: String
  node(id: ID!): Node
}

type Mutation {
  _empty: String
}

type Subscription {
  _empty: String
}
