type ChatConnection {
  edges: [ChatEdge!]!
  pageInfo: PageInfo!
}

type ChatEdge {
  cursor: String!
  node: Chat!
}

type Chat {
  id: ID!
  name: String!
}

input CreateChatInput {
  to: ID!
  name: String!
}
